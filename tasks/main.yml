---
- name: Install Fail2ban
  package: name=fail2ban
  notify: Enable Fail2ban

- name: Configure jails
  template: >
    src=jail.local.j2
    dest={{ f2b_jail_path }}/{{ item.key }}.local
  with_dict: "{{ f2b_jails }}"
  notify: Restart Fail2ban
